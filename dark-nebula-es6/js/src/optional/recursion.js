listFiles = (data, dirName) => {
  // Optional Challenge
};

listFiles = (data, dirName) => {
  // Optional Challenge
  let output = [];
  if (Array.isArray(data)) {
    if (typeOf data[0] === 'string') {
      console.log (data);
    }
  }
  else {

  }
};


permute = (arr) => {
  // Optional Challenge
};

fibonacci = (n) => {
  // Optional Challenge
};

validParentheses = (n) => {
  // Optional Challenge
};
